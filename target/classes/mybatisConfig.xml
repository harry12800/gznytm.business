<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- <properties   url="file:///D:/nytm/db.properties"></properties>
	 
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments> -->
	<mappers>
		<!--  <package name="config"/>  -->
	<!-- 	<mapper resource="config/BusinessOrderMapper.xml" />
		<mapper resource="config/ConfigMapper.xml" />
		<mapper resource="config/SerialCodeMapper.xml" />
		<mapper resource="config/IpRangeMapper.xml" />
		<mapper resource="config/DaoMapper.xml" />
 -->
	</mappers>

</configuration>